<% cache @course do %>
  <h1><%= @course.name %></h1>
  <%= @course.description %>
<% end %>

<%= link_to "Edit Course", edit_course_path(@course) %>
<%= button_to "Delete Course", @course, method: :delete, data: { turbo_confirm: "Are you sure?"} %>