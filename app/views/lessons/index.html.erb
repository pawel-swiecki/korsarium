<%= turbo_frame_tag "lessons", class: " center sidebar gutter-m-2xl" do %>
  <div class="flow">
    <h1>Lessons</h1>

    <nav class="stack">
      <% @lessons.each do |lesson| %>
        <%= link_to lesson.title, lesson, data: {turbo_frame: "lesson"} %>
      <% end %>
    </nav>
  </div>

  <%= turbo_frame_tag "lesson", class: "center flow prose", src: lesson_path(@selected_lesson) %>
<% end %>
