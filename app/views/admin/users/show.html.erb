<%= link_to "Back to all users", admin_users_path %>

<h1><%= @user.full_name %></h1>
<p><%= @user.email_address %></p>

<div>
  <%= link_to "Edit user", edit_admin_user_path(@user)  %>
  <%= button_to "Delete user", admin_user_path(@user), method: :delete, data: { turbo_confirm: "Are you sure?" } %>
</div>
