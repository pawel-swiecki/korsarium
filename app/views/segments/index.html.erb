<%= turbo_frame_tag "segments", class: "flow" do %>
  <div class="sidebar">
    <h1 style="margin-block-start: 0;">Segments</h1>

    <nav class="cluster nav">
      <ul class="cluster" role="list">
        <% @segments.each do |segment| %>
          <li><%= link_to segment.title, segment, data: {turbo_frame: "segment"} %></li>
        <% end %>
      </ul>
    </nav>
  </div>

  <%= turbo_frame_tag "segment", src: segment_path(@selected_segment) %>
<% end %>
