<wa-card>
  <div class="flow">
  <h1 class="center">Edit <%= @course.title %></h1>

  <%= render "form", course: @course %>

  <wa-button pill class="center" href="<%= korsarium_course_path(@course) %>">Cancel</wa-button>
</wa-card>
